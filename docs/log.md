# Agent Activity Log

| Timestamp | Action | Details |
|-----------|--------|---------|
| 2026-04-27 | Add todos | Added video registry md, shorts-only download, and auto-cleanup-after-ingest tasks to `docs/todo.md` |
| 2026-04-27 | Remove export module | Deleted `src/export/`; removed `cmd_export`, dispatcher entry, and docstring usage from `run.py`; updated `docs/todo.md` to drop CSV-export row and remove `export.py` / `export` command from restructure + CLI entries |
| 2026-04-27 | Add video registry | Added `src/registry.py` (append-only md helper) and seeded `docs/video_registry.md` with `file_id, url, title, duration_sec, timestamp` schema |
| 2026-04-27 | Centralize logging | Added `src/log_setup.py` configuring root logger to `yt_digest.log`; added module loggers + lifecycle log lines in `ingest`, `summarize`, `query`, `trend`, `downloader`, `URL_generation` |
| 2026-04-27 | Shorts-only search | Confirmed `URL_generation.search_top_videos` uses YouTube API `videoDuration=short`; corrected misleading comment that claimed it skipped Shorts |
| 2026-04-27 | Auto-cleanup downloader | Rewrote `downloader.download_and_ingest` to capture title+duration from yt-dlp, append a `docs/video_registry.md` row after ingest, and `os.remove` the local file. Returns ingested file_ids for orchestration |
| 2026-04-27 | Add --trendpilot CLI | Added `cmd_trendpilot` to `run.py` (`python run.py --trendpilot --topic <str> [--max <n>]`): searches YouTube → downloads (shorts) → ingests+summarizes → logs to registry → deletes file → resolves dominant detected niche → runs `run_trend` |
| 2026-04-27 | Mark all todos completed | Marked previously In Progress / Todo rows (activity docs, logging, video registry, shorts-only, auto-cleanup, trendpilot) as Completed in `docs/todo.md` |
| 2026-04-27 | Hard English filter (reverted) | Investigated non-English results — root cause: `relevanceLanguage` is only a ranking hint, not a filter. Drafted a `videos.list` post-filter on `defaultAudioLanguage`/`defaultLanguage`, but reverted at user request after surfacing risks (over-aggressive drops on videos with null language fields, `sys.exit` aborting orchestration on empty/error). Kept the corrected comment about `relevanceLanguage` being a hint. |
| 2026-04-27 | Fix JSON-mode swallowing | Niche/topic and trend/brief calls were returning `unknown` because GPT-4o-mini wrapped JSON in markdown fences and the bare `except Exception` swallowed the parse error. Added `response_format={"type": "json_object"}` to `detect_niche_topic`, `detect_trends`, and `generate_brief`, and converted the swallows to `logger.warning` with the raw payload so future failures are visible. Existing rows already stored as `niche="unknown"` will need a backfill to benefit. |
| 2026-04-27 | Backfill script (standalone) | Added `scripts/backfill_niche.py` (dry-run by default, `--apply` to write). Reads `video_summaries` rows where `niche="unknown"`, re-runs `detect_niche_topic` on the stored summary, and upserts via `insert_summary` while preserving `file_path`, `full_transcript`, `summary_embedding`, `platform`, and `ingested_at`. Not wired into `run.py` — invoked manually as `python -m scripts.backfill_niche`. |
| 2026-04-27 | Reset state | Per user request: deleted `scripts/` (backfill script), `milvus_local.db` (resets both `video_captions` and `video_summaries` collections), and `docs/video_registry.md`. Next ingest run starts from a clean slate. `yt_digest.log` retained. |
| 2026-04-27 | Registry-based URL dedup | Added `registry.loaded_urls()` parsing `docs/video_registry.md` for prior URLs. `download_and_ingest` filters out URLs already present before downloading, avoiding duplicate ingests on re-runs. `cmd_trendpilot` now falls back to running `run_trend(topic)` over existing rows when every search result was a duplicate, instead of aborting. |
| 2026-04-27 | Fix trendpilot dedup-only crash | `cmd_trendpilot` raised `ConnectionNotExistException` when every URL was a duplicate because the early-return path in `download_and_ingest` never opened the Milvus connection. Added explicit `connect()` in the no-new-ingests branch before `run_trend`. |
| 2026-04-27 | Add `clusters` CLI | New `python run.py clusters <niche>` (and `run_clusters` in `src/trend/trend.py`) prints each cluster's detected trend label + every member video's `file_id`, `topic`, and `summary`. Skips the brief-generation step from `run_trend` for inspection-only use. |
| 2026-04-27 | Make `clusters` arg optional | `python run.py clusters` (no arg) now iterates every distinct niche in `video_summaries` and runs the clusters report for each. Added `fetch_all_niches` + `run_clusters_all` in `src/trend/trend.py`. Per-niche form `clusters <niche>` still works. |
